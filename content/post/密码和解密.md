---
title: "密码和解密"
date: 2021-03-11T16:27:59+08:00
categories:
- 密码
- 笔记
tags:
- 密码
- 解密
keywords:
- 密码
- 解密
thumbnailImage: /img/95qgod.jpg
autoThumbnailImage: true
thumbnailImagePosition: "left"
coverImage: /img/43vvmv.jpg
metaAlignment: center
coverMeta: out
---

<!--more-->
# 凯撒密码
> 通过指定字母表的卫衣量加密

# 简单替换 
> 密文数量越多越容易破解，可以根据单词出现频率高低等特征进行破解

# 一次性密码本
> 因为无法判断密文的正确性，所以是根本不能破解的密码

# des加密
> 每7比特加一个错误检查的比特，构成总长64位比特的数据（实际56比特）

>Feistel网络：将要加密的数据分成左右两个部分，右侧直接输出为加密的右边部分，左侧的数据通过右侧的数据和子密钥通过轮函数生成，因为右侧的数据是没有变化的，所以要进行多轮操作，但是在两个轮之间要进行左右两部分的替换

# 三重des
> 三重des不是加密三次而是：加密-》解密-》加密，三次过程如果是相同的密钥，那么就相当于一次des加密，向des加密兼容

>解密的过程就是使用密钥、秘钥2、秘钥1执行解密-》加密-》解密的过程

# AES

>Rijindael算法可以支持的区块和密钥长度可以是32的整数倍 以128为下限256位为上限，AES的区块长度固定为128 比特，密钥长度则可以是128，192或256比特

>AES加密过程是在一个4×4的字节矩阵上运作，这个矩阵又称为“状态（state）”，其初值就是一个明文区块（矩阵中一个元素大小就是明文区块中的一个Byte）。（Rijndael加密法因支持更大的区块，其矩阵行数可视情况增加）加密时，各轮AES加密循环（除最后一轮外）均包含4个步骤：

> 1. AddRoundKey — 矩阵中的每一个字节都与该次轮秘钥（round key）做XOR运算；每个子密钥由密钥生成方案产生。

>2. SubBytes — 通过个非线性的替换函数，用查找表的方式把每个字节替换成对应的字节。

>3. ShiftRows — 将矩阵中的每个横列进行循环式移位。

> 4. MixColumns — 为了充分混合矩阵中各个直行的操作。这个步骤使用线性转换来混合每列的四个字节。
